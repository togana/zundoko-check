import { createAction } from 'redux-actions';
import * as types from '../constants/ActionTypes';

export const zun = createAction(types.ZUN);
export const doko = createAction(types.DOKO);
export const kiyoshi = createAction(types.KIYOSHI);
